// Enums for user gender
export enum Gender {
  Male = 'male',
  Female = 'female',
  Other = 'other',
}

// Enums for user marital status
export enum MaritalStatus {
  Single = 'single',
  Married = 'married',
  Divorced = 'divorced',
  Widowed = 'widowed',
}
